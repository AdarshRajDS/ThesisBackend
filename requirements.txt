# PyTorch: use the default PyPI wheel (works on macOS and Linux).
# The +cpu extra-index URL often fails on macOS or Python 3.13; Docker still builds with plain torch==2.2.2.
fastapi
uvicorn[standard]

langchain
langchain-community
langchain-openai
langchain-chroma
langchain-huggingface



chromadb>=1.5.0


sentence-transformers==2.7.0
transformers==4.41.2
accelerate==0.30.1

torch==2.2.2

pymupdf
pillow
# Pinned: unpinned headless often falls back to sdist on macOS and fails without CMake.
opencv-python-headless==4.8.1.78

python-dotenv
python-multipart

tqdm
numpy==1.26.4
pandas
scikit-learn

# Object storage (S3-compatible)
minio
supabase
pytest>=8.0
httpx>=0.24.0
python-docx>=1.1.0
